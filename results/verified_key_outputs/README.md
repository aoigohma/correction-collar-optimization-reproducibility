# Verified reference outputs

The 13 non-LME reference CSVs are used by the current no-LME manuscript validator (`validate_against_verified` by default).

`Figure4_LME_coefficients.csv` and `Figure4_LME_model_comparison.csv` are **historical files only**, retained from v1.0.0 for provenance. They are not analyses reported in the final manuscript, and the updated validator excludes them unless explicitly requested with `IncludeHistoricalLME=true`.
